services:
  react-prod:
    build:
      context: .
      dockerfile: Dockerfile
    image: react-prod-image
    ports:
      - "8080:8080"

  react-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: react-dev-image
    ports:
      - "5173:5173"
    develop:
      watch:
        - action: sync
          path: .
          target: /app